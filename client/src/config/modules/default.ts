import { TemplateConfig } from './types';

const defaultTemplate: TemplateConfig = {
  background: "bg-gray-100 home-theme",
  layout: "vertical",
  accentColor: "border-primary",
  cardBackground: "bg-white dark:bg-gray-900",
  iconBackground: "bg-primary/15 dark:bg-primary/30",
  textColor: "text-primary dark:text-blue-300",
  buttonColor: "bg-primary hover:bg-primary/90 dark:bg-blue-600 dark:hover:bg-blue-700",
};

export default defaultTemplate;